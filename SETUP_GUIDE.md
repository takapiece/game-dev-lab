# 🚀 ゲームスタジオ & 学習ダッシュボード 起動マニュアル (Windows / Mac 対応)

本リポジトリは、**Windows** と **Mac (macOS / MacBook)** のどちらの環境でも100%同じように動作する完全クロスプラットフォーム設計になっています。

生徒さんや講師の方が迷わず起動できるよう、OS別の手順をまとめました。

---

## 🛠️ 事前準備（共通）

ゲームを動かすために **Node.js** が必要です。

1. **Node.js の確認**:
   - ターミナル（Mac）または PowerShell（Windows）を開き、以下のコマンドを入力します。
   ```bash
   node -v
   npm -v
   ```
   - バージョン番号（例: `v20.x.x` など）が表示されれば準備完了です。
   - ※ まだインストールしていない場合は、[Node.js 公式サイト (LTS版)](https://nodejs.org/) からインストーラーをダウンロードしてインストールしてください。

---

## 🪟 Windows での起動手順

### 1. ターミナルを開く
- **VS Code を使っている場合:** 画面上部メニューの `ターミナル` $\rightarrow$ `新しいターミナル` を開きます。
- **通常の起動:** スタートメニューから `PowerShell` または `コマンドプロンプト` を開きます。

### 2. ゲームフォルダへ移動して起動
```powershell
# 1. ゲームのディレクトリへ移動
cd games/01_hoops_3d

# 2. 必要な部品のインストール（初回のみ）
npm install

# 3. 開発サーバーの起動
npm run dev
```

### 3. ブラウザで開く
起動すると自動的にブラウザが開きます。自動で開かない場合は、ブラウザ（Chrome / Edge 等）で以下のアドレスを開いてください：
👉 **[http://localhost:5173/](http://localhost:5173/)**

### 4. サーバーの終了方法
キーボードの **`Ctrl` + `C`** を押し、`バッチ ジョブを終了しますか (Y/N)?` で **`Y`** を押すと停止します。

---

## 🍎 Mac (macOS / MacBook) での起動手順

### 1. ターミナルを開く
- **VS Code を使っている場合:** 画面上部メニューの `ターミナル` $\rightarrow$ `新しいターミナル` を開きます。
- **通常の起動:** `Finder` $\rightarrow$ `アプリケーション` $\rightarrow$ `ユーティリティ` $\rightarrow$ `ターミナル.app` を開くか、Spotlight（`Cmd` + `Space`）で「ターミナル」と検索して開きます。

### 2. ゲームフォルダへ移動して起動
```bash
# 1. ゲームのディレクトリへ移動
cd games/01_hoops_3d

# 2. 必要な部品のインストール（初回のみ）
npm install

# 3. 開発サーバーの起動
npm run dev
```

### 3. ブラウザで開く
ブラウザ（Google Chrome / Safari 等）で以下のアドレスを開きます：
👉 **[http://localhost:5173/](http://localhost:5173/)**

### 4. サーバーの終了方法
キーボードの **`Control` + `C`** を押すと停止します。

---

## 🎮 画面構成とURL一覧

サーバーを起動すると、以下のすべての画面を自由に移動・体験できます：

```text
┌─────────────────────────────────────────────────────────────┐
│ 🏠 ポータル目次画面 (http://localhost:5173/)                 │
├───────────────────────────────┬─────────────────────────────┤
│ 📘 統合学習ダッシュボード     │ 🏀 フェーズ別ゲームプレイ   │
│ (http://localhost:5173/       │                             │
│  dashboard/index.html)        │ ● Phase 1: 3Dソロ練習       │
│                               │   (/phases/phase_01_...)    │
│ ・コード閲覧 & ホバー解説     │                             │
│ ・数学・物理の公式図解        │ ● Phase 2: 1v1 AI対戦       │
│ ・AIへのプロンプト指示ガイド  │   (/phases/phase_02_...)    │
│ ・トラブル解決事例            │                             │
└───────────────────────────────┴─────────────────────────────┘
```

* **🏠 ポータル目次画面**: [http://localhost:5173/](http://localhost:5173/)
* **📘 学習ダッシュボード**: [http://localhost:5173/dashboard/index.html](http://localhost:5173/dashboard/index.html)
* **🏀 Phase 1（ソロ練習）**: [http://localhost:5173/phases/phase_01_3d_mock/index.html](http://localhost:5173/phases/phase_01_3d_mock/index.html)
* **🔴 Phase 2（1v1 AI対戦）**: [http://localhost:5173/phases/phase_02_1v1_defender/index.html](http://localhost:5173/phases/phase_02_1v1_defender/index.html)

---

## ❓ よくある質問・トラブルシューティング

### Q1. `npm: コマンドが見つかりません` / `command not found: npm`
* **原因:** Node.js がインストールされていないか、パスが通っていません。
* **対策:** [Node.js 公式サイト](https://nodejs.org/) から最新の LTS版 をインストールし、ターミナルを一度閉じて再起動してください。

### Q2. 画面を開いても音が鳴らない
* **原因:** モダンブラウザのセキュリティ仕様により、画面操作前の自動音声再生がブロックされています。
* **対策:** 画面上の「▶ GAME START」ボタンを押すか、画面内を一度クリック/タップすると効果音が有効になります。

### Q3. ポート `5173` がすでに使われていると表示される
* **原因:** 以前起動したサーバーがバックグラウンドで動いている可能性があります。
* **対策:** Vite が自動的に `http://localhost:5174/` などの空いているポートで起動しますので、ターミナルに表示されたURLを開いてください。
