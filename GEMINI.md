# ゲーム開発スタジオ & 教育カリキュラム運用ルール (Workspace Guidelines)

本ワークスペースは、プログラミング教室の生徒さんへ「ゲーム開発の手順・技術選定・エラー解決の思考プロセス」を伝えるための**マルチゲーム開発ラボ（教材リポジトリ）**です。
複数のゲームプロジェクト（`games/<game_name>/`）を順次開発・拡張していきます。

開発およびAIペアプログラミングを行う際は、以下の原則を遵守してください。

---

## 1. 複数ゲーム（マルチプロジェクト）運用の原則
1. **プロジェクトの独立性 (`games/<game_name>/`)**:
   - 各ゲームは `games/<game_name>/` 配下に完全に独立して格納する（ソースコード、アセット、専用ドキュメント、`package.json`）。
2. **ゲームごとのドキュメント管理**:
   - 各ゲームディレクトリ内の `docs/` にて、「仕様書（`00_roadmap`）」「セッション手順（`01_sessions`）」「エラーログ（`02_troubleshooting`）」「アーキテクチャ解説（`03_architecture`）」を管理する。
3. **全体ポータルの更新 (`docs/README.md`)**:
   - 新しいゲームを追加した際は、ルートの `docs/README.md` のゲーム一覧を更新する。

---

## 2. Windows / Mac 完全互換・相対パス運用の原則 (Cross-Platform)
1. **絶対パスの完全禁止**:
   - ドキュメント内のリンクやコード内参照には、`D:` や `C:`、`/Users/...` などのOS依存絶対パスを含めず、**必ず相対パス（`./...`, `../...`）で記述**する。
2. **パス区切り文字のスラッシュ（`/`）統一**:
   - マークダウンリンクやインポート文には、Win/Mac共通で動作するスラッシュ（`/`）を使用する。

---

## 3. コード設計 & 生徒指導方針
1. **生徒目線の可読性と自己説明性**:
   - 変数名・関数名は初学者が読んでも役割が直感的に伝わる命名にする。
   - 3D空間の数学（ベクトル、三角関数、放物線等）には日本語コメントで意図を記載する。
2. **エラーと意思決定の記録**:
   - 発生したエラーは `02_troubleshooting/error_log.md` に学び付きで記録する。
   - 大きな技術選定・方針決定は `00_roadmap/decisions.md` に ADR として記録する。
3. **防御的プログラミング (Defensive Programming)**:
   - オブジェクトの初期化順序依存によるクラッシュを防ぐ安全ガードを適切に配置する。
